{
  "name": "adws",
  "private": true,
  "type": "module",
  "description": "SSSF control plane — deterministic TypeScript workflows that own sequencing, retries, and acceptance, with coding agents as bounded nodes inside them.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.7.2"
  }
}
